N = int(input())
ropeWei = [int(input()) for _ in range(N)]
ropeWei.sort()
listLen = len(ropeWei)
candiList = [0 for _ in range(N)]
candiList[0] = ropeWei[0] * N

for idx in range(1,listLen):
    if ropeWei[idx] == ropeWei[idx-1]:
        candiList[idx] = candiList[idx-1]
    else:
        candiList[idx] = ropeWei[idx] * (listLen - idx)

print(max(candiList))

이 문제는 쉽게 해결했다.
로프들을 임의로 선택하여 들 수 있는 무게의 최대 중량을 찾는 문제이다.
로프리스트를 정렬하여서, 최대 중량의 후보들을 전수 조사하였다.
